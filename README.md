# efento_temp Config

1. Install Git
```bash
sudo apt update
```
```bash
sudo apt install git
```

2. Install Wireguard

```bash
sudo apt install wireguard
```
```bash
sudo nano /etc/wireguard/wg0.conf
```
